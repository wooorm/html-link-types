{
  "name": "html-link-types",
  "version": "1.2.0",
  "description": "List of link types as used in HTML",
  "license": "MIT",
  "keywords": [
    "html",
    "dom",
    "link",
    "type",
    "rel"
  ],
  "repository": "wooorm/html-link-types",
  "bugs": "https://github.com/wooorm/html-link-types/issues",
  "author": "Titus Wormer <tituswormer@gmail.com> (http://wooorm.com)",
  "contributors": [
    "Titus Wormer <tituswormer@gmail.com> (http://wooorm.com)"
  ],
  "main": "index.json",
  "files": [
    "index.json"
  ],
  "dependencies": {},
  "devDependencies": {
    "bail": "^1.0.2",
    "browserify": "^16.0.0",
    "concat-stream": "^1.6.0",
    "esmangle": "^1.0.1",
    "hast-util-select": "^1.0.1",
    "hast-util-to-string": "^1.0.0",
    "rehype-parse": "^4.0.0",
    "remark-cli": "^5.0.0",
    "remark-preset-wooorm": "^4.0.0",
    "tape": "^4.4.0",
    "unified": "^6.1.5",
    "xo": "^0.20.0"
  },
  "scripts": {
    "build-md": "remark . -qfo",
    "build-generate": "node build",
    "build-bundle": "browserify index.json --bare -s htmlLinkTypes > html-link-types.js",
    "build-mangle": "esmangle html-link-types.js > html-link-types.min.js",
    "build": "npm run build-generate && npm run build-md && npm run build-bundle && npm run build-mangle",
    "lint": "xo",
    "test-api": "node test.js",
    "test": "npm run build && npm run lint && npm run test-api"
  },
  "xo": {
    "space": true,
    "esnext": false,
    "rules": {
      "no-var": "off",
      "prefer-arrow-callback": "off"
    },
    "ignores": [
      "html-link-types.js"
    ]
  },
  "remarkConfig": {
    "plugins": [
      "preset-wooorm"
    ]
  }
}
